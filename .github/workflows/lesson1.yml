name: Lesson 2

on:
  push:

jobs:

  first-job:
    runs-on: ubuntu-latest

    steps:
      - run: echo "This is the first job"

  second-job:
    needs: first-job
    runs-on: ubuntu-latest

    steps:
      - run: echo "This runs after first-job"
